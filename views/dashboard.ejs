<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome to Say Ya Mind</title>
  <style>
    /* General Reset */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    /* Body and background styling */
    body {
      font-family: 'Poppins', sans-serif;
      background: url('https://i.imgur.com/r7I7MUC.jpeg') center/cover no-repeat;
      color: white;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      text-align: center;
      background-size: cover;
      background-position: center;
    }

    /* Main container */
    .container {
      background: rgba(0, 0, 0, 0.6); /* Dark overlay to make text more readable */
      padding: 40px;
      border-radius: 15px;
      box-shadow: 0 10px 15px rgba(0, 0, 0, 0.5);
    }

    /* Heading styles */
    h1 {
      font-size: 3em;
      color: #FFD700; /* Gold color */
      margin-bottom: 20px;
      font-family: 'Cursive', sans-serif;
      text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
    }

    /* Link styling */
    a {
      color: #FFD700; /* Gold color */
      font-size: 1.2em;
      text-decoration: none;
      margin: 0 10px;
      font-weight: bold;
      transition: color 0.3s ease;
    }

    /* Hover effect for links */
    a:hover {
      color: #ff8c00; /* Orange color on hover */
    }

    /* Additional paragraph styling */
    p {
      font-size: 1.2em;
      margin-top: 20px;
      font-family: 'Arial', sans-serif;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Welcome to Say Ya Mind</h1>
    <p><a href="/register">Register</a> | <a href="/login">Login</a></p>
  </div>
</body>
</html>
